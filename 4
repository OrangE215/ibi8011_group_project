transcription factor_1 = "TATGTAAATA"
transcription factor_2 = "GTAAACA"

# Use the function to check whether they can bind simultaneously

define check(DNA_sequences):
    sum1 = []
    mark1 = ""
    sum2 = []
    mark2 = ""
    for sum1 in lenth(DNA_SE)
        if DNA_SE(sum_1) == "TATGTAAATA"
            mark1 = length(DNA_SE(sum_1))
            continue;
            
   for sum2 in lenth(DNA_SE)
        if DNA_SE(sum_2) == "GTAAACA"
            mark2 = length(DNA_SE(sum_2))
            continue;
          
if mark1 - mark2 != 4
    print("True")
elif mark1 - mark2 == -4
    print("True")
else
    print("False")
